[pytest]
markers =
    smoke: critical smoke tests
    regression: broader regression coverage
    ui: user interface tests
#---  I added this on march 26.2026
    api: API tests
    ddt: Data-driven tests
    hybrid: UI + API combined tests

